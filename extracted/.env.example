# Copiez ce fichier en `.env.local` et renseignez l'URL PostgreSQL.
# Elle n'est requise que pour l'endpoint /api/health (le jeu tourne côté client).
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db
